version: '3.8'
services:
  app:
    build: .
    command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "80"]
    ports:
      - 80:80
    healthcheck:
      test: curl -sS http://127.0.0.1/docker/healthcheck || echo 1
      timeout: 10s
      retries: 3